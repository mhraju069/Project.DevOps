apiVersion: v1
kind: Service
metadata:
  name: {{ include "site1.fullname" . }}
  labels:
    {{- include "site1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "site1.selectorLabels" . | nindent 4 }}
