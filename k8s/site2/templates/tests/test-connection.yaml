apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "site2.fullname" . }}-test-connection"
  labels:
    {{- include "site2.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "site2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
